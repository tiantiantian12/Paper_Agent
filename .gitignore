# ============================================================ Paper Agent Server（服务端）
# 按你的要求：**不排除任何密钥**（config.json 里的 SMTP 密码、各家 sk- key、
# 看板口令 admin_access_token 都直接提交到私人仓库）。
# 这里只忽略「本机跑出来的运行数据」与缓存类文件。

# ---------------------------------------------------------------- Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.pyd
*_pycache_*

# 打包 / 构建产物
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/

# ---------------------------------------------------------------- 虚拟环境
.venv/
venv/
env/
ENV/
.conda/
.python-version

# ---------------------------------------------------------------- 测试 / 静态检查缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/
.pytype/
.coverage
coverage.xml
htmlcov/
.tox/
.nox/

# ---------------------------------------------------------------- 运行时数据（本机生成）
# data/ 是 sqlite 库（用户、密钥池、模型、请求日志）＋ WAL 文件，随运行一直变；
# 表结构由 app/database.py 的 init_db() 在启动时自动建，新克隆直接跑 run.py 就行。
# 想连库一起留档 → 删掉下面 data/ 这两行。
data/
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# 运行日志
*.log
logs/

# ---------------------------------------------------------------- IDE / 编辑器 / 系统
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
Desktop.ini

# ---------------------------------------------------------------- 临时文件（调试脚本等）
_tmp_*
_*.tmp
